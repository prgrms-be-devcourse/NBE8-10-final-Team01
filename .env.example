# Copy this file to `.env` and customize values for your local machine.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=back
DB_USERNAME=back
DB_PASSWORD=change-this-local-password

# judge0 ec2 ip
JUDGE0_EC2_IP=NEED_TO_SET